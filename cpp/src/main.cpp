#include "cal.h"

int main() {
    auto sum = add(1, 2);
    std::cout << "sum: " << sum << std::endl;
    // auto ret = divide(2, 0);
    std::cout << "sum: " << sum << std::endl;
    std::cout << "sum: " << sum << std::endl;
    std::cout << "sum: " << sum << std::endl;

    return 0;
}